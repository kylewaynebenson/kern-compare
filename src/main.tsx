import React from 'react';
import ReactDOM from 'react-dom/client';
import KerningComparison from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KerningComparison />
  </React.StrictMode>
);